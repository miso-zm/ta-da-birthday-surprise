# 视觉与交互设计契约

**状态：** Draft 0.1，等待用户审核

## 设计方向

这是一套温暖、轻松、略带稚拙感的插画型生日体验：暖奶油背景、珊瑚红主色、大圆角白卡、粗棕色描边、平涂手绘插画和少量纸张质感。

视觉负责传达“朋友亲手准备的生日惊喜”，不是科技产品、效率工具或 AI 产品。

## 设计令牌

第一版建议值，审核后由总控写入 `src/styles/tokens.css`：

| 用途 | 建议值 |
|---|---|
| 页面背景 | `#FFF8EB` |
| 抬升纸面 | `#FFFDF7` |
| 主强调珊瑚红 | `#E95F58` |
| 珊瑚深色 | `#BD433E` |
| 主文字暖棕 | `#4A382F` |
| 描边深棕 | `#5F4639` |
| 次级文字 | `#76645A` |
| 奶油黄 | `#F7CF68` |
| 柔粉 | `#F2B7AD` |
| 鼠尾草绿 | `#A8BD82` |
| 淡天蓝 | `#9BC9D5` |

珊瑚红是唯一强强调色；黄色、粉色、绿色和蓝色只用于插画、状态和小装饰。

## 形状规则

- 页面主卡片：24-32px 圆角，默认 28px。
- 按钮与输入：16-20px 圆角，默认 18px。
- 标签或小状态：12-14px 圆角；不把所有内容做成胶囊。
- 主卡片可以使用 2-3px 暖棕描边和同色向下 4-8px 的实体投影。
- 不使用纯黑阴影、漂浮玻璃卡或不同页面各自一套圆角。

## 排版

- 中文优先使用系统圆体感无衬线栈；不依赖在线字体完成首屏。
- 标题粗、紧凑、短句化；正文保持自然，不写刻意卖萌或 AI 式抒情文案。
- 标题 28-38px，正文 14-16px，辅助信息不小于 12px。
- 正文行高至少 1.5，长祝福不使用全大写或过度字距。
- 允许一处手绘荧光笔式标题强调，不在每个区块重复。

## 插画与图标

- 插画：粗暖棕描边、平涂色块、稍微笨拙但构图清楚。
- 场景物体必须易识别、易点击，不用写实照片冒充游戏资产。
- UI 图标统一使用一个图标家族，优先 Phosphor。
- 不用 Emoji 代替主要操作图标。
- 最终生成资产必须保存到项目内并记录用途、尺寸和生成提示词。

## 页面与组件

- 主要验收画布为 390×844px，内容适配 360-430px。
- 桌面端将移动体验居中并自然扩展，不绘制假的手机边框。
- 每屏只保留一个主要动作；相同意图使用相同按钮文案。
- 卡片只用于真实层级：贺卡、游戏场景、礼物和分享链接。
- Scrapbook 始终保持 3:4，可组合照片槽，不变成自由编辑器。

## 动效顺序

动效在阶段 E 才加入，并必须表达以下至少一种含义：

- 层级：引导用户关注下一步
- 反馈：确认点击、找到或解锁成功
- 状态变化：展示礼盒打开或内容出现
- 故事节奏：让 Opening 和 Reveal 有情绪递进

只动画 `transform` 和 `opacity`。所有非必要动效尊重 `prefers-reduced-motion`，并保留静态完成态。

## 可访问性

- 正文与背景满足 WCAG AA 对比度。
- 所有主要按钮具有可见焦点、按下反馈和不换行标签。
- 点击热区至少 44×44px。
- 不能只用颜色表达游戏结果或完成状态。
- 插画提供描述性替代文本；纯装饰元素从辅助技术中隐藏。

## 明确禁止

- 紫蓝科技渐变
- 玻璃拟态、毛玻璃浮层
- 霓虹、外发光和粒子背景
- 大量渐变文字
- 默认 SaaS 仪表盘风格
- 六个场景分别发展不同视觉系统
- 未经审核直接制作复杂粒子、3D 或滚动劫持动画
- 用通用占位图长期冒充最终模板或游戏场景

## 视觉验收

- 五秒内能感受到“温暖、私人、生日”。
- 珊瑚红在全部页面保持相同语义。
- 主按钮、卡片、输入和插画描边遵守同一形状体系。
- 关闭动画后仍能完成全部流程。
- 任意单个模块替换后，不破坏整体视觉语言。
