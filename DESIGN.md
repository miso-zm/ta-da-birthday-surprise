# Ta-da! 视觉与交互设计契约

**状态：** Working 0.7（“轻纸感生日手帐”方向已通过视觉样图审核，等待页面实现）

## 设计方向

正式方向命名为 **“轻纸感生日手帐（Soft Paper Birthday）”**：以轻盈、清楚的现代移动端界面作为骨架，叠加旧纸、折纸、彩铅和轻微印刷磨损的手作生日卡片气质。目标不是复古海报或满屏剪贴簿，而是让用户感觉正在打开一份被朋友认真准备过的生日惊喜。

界面秩序与情绪材质的建议比例为：约 70% 轻盈移动端 UI，约 30% 旧纸手作气质。普通 UI 依靠留白、色差和极弱阴影分层；纸纹、折角、胶带、标签和手绘线只出现在具有叙事意义的场景中。

**UI 与插画必须区分：** 按钮、输入框、导航和普通内容卡片默认无深色描边，优先无边框，确需分隔时才使用 1px 暖米色细线。Tada、游戏物件和装饰插画使用细到中等、略不均匀的暖棕手绘线；大型轮廓可以略重，内部结构线必须更轻。全站不再把“粗棕描边”作为插画的统一默认风格。

视觉负责传达“朋友亲手准备的生日惊喜”，不是科技产品、效率工具或 AI 产品。

## 纸张与手作质感

- 页面画布保持干净的暖灰奶油色，可使用极轻纸张颗粒、低对比度染色和淡铅笔涂鸦；纹理不得影响文字清晰度或形成明显脏污。
- 折角只用于占据页面主要视觉面积的大型叙事纸张，例如 Opening 主卡、完整贺卡 / Scrapbook 模板、游戏主场景、Sender 完整预览摘要或全页恢复卡。它表达“正在打开一张重要纸张”，不是普通卡片的装饰。
- 小型提示条、Tada 陪伴条、列表项、选择卡、加载卡、成功提示、礼物信息小卡、输入框和按钮一律不使用折角。即使它们使用纸面背景，也只通过色差、圆角或弱阴影分层。
- 同一屏最多出现一个明显折角；若页面已有大型折角主卡，其周围所有附属小卡必须保持平整。
- 纸张悬浮使用宽、淡、软的阴影；不使用深色硬边投影或 4–8px 实体下沉效果。
- 场景插画优先使用浅彩铅、淡水彩或轻微套印质感；颜色需要温暖但不泛黄，不使用重度做旧、褐色滤镜或强噪点。
- 页面只设置一个主要视觉焦点：一张场景插画、一件纸张内容或一个 Tada 动作；其余 UI 保持安静。

## Ta-da! 与 Tada

- 产品名称固定写作 `Ta-da!`，不得擅自改成 TADA、Tada App 或科技品牌式字标。
- 角色名为 `Tada`：温暖、调皮、治愈、喜欢收藏心意和送出惊喜。
- 角色使用奶油白身体、暖棕描边、珊瑚粉脸颊、薄荷绿小角和心意袋。
- 已有角色设定图足以进行场景规划；正式分层动画仍需要透明分层资产或经过用户审核的重建素材。
- Tada 的出现必须承担引导、反馈、陪伴或惊喜中的至少一种作用，不作为无意义的大面积装饰。
- 标准识别锚点为不对称薄荷绿小角、奶油身体、暖棕描边和珊瑚脸颊；角色色板独立于页面 UI 令牌，不能因页面状态随意换色。
- 失败或答错状态使用好奇、思考和鼓励的表情，不哭泣、不责怪用户、不使用嘲讽文案。
- Gift Reveal 中隐藏心意袋，只保留礼盒、Tada 和礼物信息，避免两个“礼物容器”抢夺注意力。

P0 静态与轻动效优先复用五种身体母版：等待、提问 / 提示、庆祝、手持 / 信封、探头。不同场景主要替换表情、手持物和少量装饰，不为每个页面重新设计一套角色结构。

小型提示条统一使用可复用 `TadaCompanion` 组件，不再用 CSS 基础图形拼脸。默认 `guide` 状态为用户已审核的“中性招手”透明角色：从提示条边缘探头，一手扶边、一手招手；显示尺寸以 52–60px 为主，必须保留薄荷小角、奶油身体、暖棕线和珊瑚脸颊的识别度。组件可按语义切换 mood、文案和尺寸，但角色不得挤压正文、成为独立厚重徽章或无意义循环动效。

P0 角色场景：

1. Opening：探头、眨眼、心意袋轻弹。
2. Unlock：等待、疑惑或失败提示、成功庆祝。
3. Birthday Card：递出信封的静态或轻量动作。
4. Scrapbook：从相册后探头或整理照片。
5. Gift Reveal：礼盒打开、Tada 升起并出现 `Ta-da!`。
6. Share：挥手或抱爱心。

P0 正式动效只包括 Opening、Unlock 成功和 Gift Reveal。Card、Scrapbook、Share、Loading 与 Error 使用静态姿势或轻量位移、缩放和表情切换。

## D-062 生日主角海报

- 入口位于 Sender 心意内容步骤，Card 与 Scrapbook 共用；作为可选区，不新增第六步。
- 编辑预览与 Receiver 成品为 1:1。固定图层不跟随人物拖动；人物只允许受限拖动、缩放与重置，不提供自由画布、旋转控件或生日帽定位。
- 人像保持原色，使用一体化不规则底部蒙版、暖白内覆盖描边与极淡阴影；撕纸效果属于人物贴纸本身，不能在模板前景额外横盖一条纸。
- 气球派对使用暖色气球、蛋糕、礼盒与星星；蓝调生日使用蓝白纸张、蜡笔、双星、礼物盒、银色星光、疏朗彩带与单蜡烛蛋糕。两款均无年龄数字、无帽。
- 390×844 为主验收；页面两侧至少 20px，海报内容自身保留安全边距，主按钮继续使用全局 52px 胶囊规范。Receiver 5/5 只替换插画区域，不放大标题、正文或按钮。

## 颜色令牌

参考图中的色板作为 UI 唯一基础色板。颜色名称表达用途，不用具体色名直接散落在组件中。

### 品牌与内容色

| Token | 色值 | 用途 |
|---|---|---|
| `--color-coral` | `#FA907B` | 唯一品牌珊瑚色：高亮、选中描边、浅色混合与装饰 |
| `--color-coral-action` | `#FA907B` | 主按钮与选中 Icon；与品牌珊瑚保持同一色值 |
| `--color-yellow` | `#FFBE3D` | 星星、奖励、提示性插画 |
| `--color-mint` | `#7ED9C8` | 成功倾向、轻游戏、Gift Reveal 辅助色 |
| `--color-sky` | `#A8D8FF` | 信息、游戏元素、轻背景 |
| `--color-lilac` | `#C7B1FF` | 音乐、礼物内容和少量梦幻场景 |

### 中性色

| Token | 色值 | 用途 |
|---|---|---|
| `--color-ink-strong` | `#2B2116` | 标题、主要图标、Tada 插画深描边 |
| `--color-ink` | `#5B4636` | 正文、按钮文字、普通图标 |
| `--color-ink-muted` | `#9A8774` | 辅助说明、时间、未选中导航 |
| `--color-line` | `#F3E9DA` | UI 边框、分割线、输入框描边 |
| `--color-canvas` | `#F8F4EE` | 页面统一暖奶油背景 |
| `--color-surface` | `#FFFFFF` | 卡片、输入框、底部导航 |
| `--color-on-light` | `#2B2116` | 浅色表面上的主要文字和图标 |
| `--color-on-dark` | `#FFFDF9` | 深色表面上的反白文字和图标 |

### 功能色

| Token | 色值 | 用途 |
|---|---|---|
| `--color-success` | `#6FCF97` | 完成、解锁成功 |
| `--color-warning` | `#FFC36B` | 提醒、等待 |
| `--color-danger` | `#FF686B` | 错误、删除 |
| `--color-info` | `#8AB4F8` | 信息提示 |

珊瑚红仍是唯一主操作色。黄色、薄荷绿、天蓝和淡紫用于场景分类、状态、插画与小面积背景，不与主按钮争夺注意力。页面不使用纯黑，正文优先使用暖棕中性色。

### 表面与文字配对

- 深色填充表面统一使用反白文字与反白图标，即 `--color-on-dark`；适用于深色标签和深色插画信息块。
- 浅色填充表面统一使用深色文字与深色图标，即 `--color-on-light`；适用于白色、暖奶油、浅珊瑚、浅黄、浅薄荷、浅蓝和浅紫表面。
- 组件内部的标题、辅助文字和图标必须跟随其表面使用同一明暗体系，不得在深色选中块上保留灰色说明文字。
- 全站品牌珊瑚统一为 `#FA907B`，不再设置深红、深珊瑚或另一档粉红品牌色。主按钮、选中 Icon 与选中卡描边都直接使用该色；选中卡本身仍使用接近白色的极浅粉纸面，不整块填充珊瑚色。
- 根据当前视觉决定，`#FA907B` 主按钮和 Icon 继续使用 `--color-on-dark` 反白内容。该组合对比度约为 2.17:1，未达到 WCAG AA；这是当前已知风险，不得在验收记录中写成已达标。若后续优先修复可访问性，只能在“改用深色文字”或“重新批准更深的操作色”中二选一，不得私自增加新的红色。
- 普通文字与背景至少满足 WCAG AA 4.5:1；大号文字至少 3:1。设计稿和实现都不得只凭“看起来够深”判断文字颜色。

## 形状规则

- `--radius-sm: 12px`：输入框、标签、小游戏格子、小按钮。
- `--radius-md: 20px`：普通内容卡、弹层、底部导航。
- `--radius-lg: 28px`：主场景卡、贺卡、礼物容器。
- `--radius-round: 999px`：主 CTA、头像、计数徽标；仅在控件确实需要胶囊或圆形时使用。
- UI 普通表面优先无边框；确需分隔时使用 1px `#F3E9DA`。选中状态可使用 1.5–2px 珊瑚色细边框，也可直接使用深色填充与反白文字，但不得叠加厚边框和实体阴影。
- 不同页面不得自行创造新的圆角层级。

## 阴影

阴影只表达层级，不模拟厚重玩具按钮：

| Token | 建议值 | 用途 |
|---|---|---|
| `--shadow-card` | `0 4px 20px rgba(43, 33, 22, 0.06)` | 普通卡片 |
| `--shadow-raised` | `0 8px 24px rgba(43, 33, 22, 0.08)` | 当前重点内容、弹层 |
| `--shadow-pressed` | `0 2px 8px rgba(43, 33, 22, 0.06)` | 按钮按下或较低层级 |

禁止把深棕色实体位移阴影应用到所有按钮、输入和卡片。游戏棋子或插画道具如需实体感，必须作为场景资产单独定义。

## 排版

- 全站所有可编辑 UI 文字统一使用圆体，包括品牌字标、标题、正文、按钮、输入框、标签、数字和英文辅助信息；不得在局部退回普通黑体或系统无衬线字体。
- 全局字体入口固定为 `--font-rounded`。正式选择为方案 7「源泉圆体（GenSen Rounded / GenSenMaruGothic TW）」；本地 Demo 优先读取已安装字体，Vercel 发布前将 OFL 1.1 字体文件压缩为 Web Font 并随站点发布。组件不得自行声明另一套字体。
- 全站字重只允许三档：正文 `--font-weight-regular: 500`、强调 `--font-weight-medium: 600`、标题与主要操作 `--font-weight-bold: 700`。不得在组件中使用 400、750、800、900 等额外字重。
- 页面标题使用 700；正文和辅助文字统一以 500 为基础；区块标题、标签或次级强调使用 600。辅助信息主要通过字号和 `--color-ink-muted` 建立层级，不再使用过细字重。
- Sender 五步流程页直接以唯一的大标题开始，不在标题上方重复放置粉色眉题、小标题或阶段口号。步骤身份已经由顶部 `1/5–5/5` 进度表达；页面层级固定为“大标题 → 一句说明 → 表单内容”。恢复草稿、错误状态等非流程页面可按语义使用必要的状态标签。
- 品牌字标可以比正文更活泼，但仍来自同一圆体系统，不另外混用手写体。
- 例外：Birthday Card「递到你手里的信」固定使用「内海字体 Light」作为局部手写字体，作用于卡片内的收件人、祝福正文与署名，营造亲手写下的短笺感；不得使用粗重字形、文字阴影或描边。10–45 字短祝福在 390px 手机宽度下最高为 27px，不因字数继续减少而放大；46–200 字按内容密度逐档缩小。卡片外的标题、按钮及其他页面仍使用全局圆体。
- 移动端页面标题 28–34px；区块标题 18–22px；正文 14–16px；辅助文字 12–13px。
- 正文行高 1.55–1.7；标题短句化，不使用过度字距和连续全大写。

## 插画与图标

- 插画：浅彩铅、淡水彩或轻套印色块，搭配细到中等、略不均匀的暖棕手绘线；外轮廓与内部线条必须有粗细层级。
- 场景物体必须易识别、易点击，不用写实照片冒充游戏资产。
- 大型场景中的可点击物件优先直接成为热点，通过轻抬、选中圈或状态文字反馈，不额外套一层厚重悬浮按钮。
- UI 图标统一使用一个图标家族，优先 Phosphor。
- 不用 Emoji 代替主要操作图标。
- 最终生成资产必须保存到项目内并记录用途、尺寸和生成提示词。

## 基础组件

- 主要验收画布为 390×844px，内容适配 360-430px。
- 桌面端将移动体验居中并自然扩展，不绘制假的手机边框。
- 每屏只保留一个主要动作；相同意图使用相同按钮文案。

### 按钮

- 主按钮：高度 48–52px，圆角胶囊，统一使用 `#FA907B` 与反白文字；默认无深色描边，只使用柔和按压阴影。当前白字对比度风险见“表面与文字配对”。
- 次按钮：白色或画布色背景、1px 暖米色边框、暖棕文字。
- 图标按钮：44×44px 最小点击区；礼物等高优先级动作可使用珊瑚底色。
- 按下反馈使用轻微缩放或下移 1px，不再使用 4–8px 厚重实体位移。
- 一屏只出现一个实心主按钮。

### 输入框

- 高度至少 48px，12px 圆角，1px `--color-line` 边框，白色背景。
- 水平内边距 14–16px；正文 16px，避免 iOS 自动放大。
- 聚焦使用珊瑚色边框和低透明度外环，不使用深棕粗框。
- 尾部图标放在输入框内部，使用统一线性图标，不叠加浮动控件。
- 错误状态使用危险色边框加可读错误文字，不能只变颜色。

### 选择卡与标签

- 选择卡采用“左侧文字 + 右侧缩略图 / 图标”布局。未选中卡使用白色纸面与 1px 暖米色边界；选中卡使用 `12%–14%` 的 `#FA907B` 混白极浅粉纸面、2px `#FA907B` 描边和深色文字，右侧 Icon 从空心变为 `#FA907B` 填充，并使用白色圆体勾。整张珊瑚实心卡不作为成组选项的默认方案。
- 选择勾必须使用统一的圆润线性 Icon：线帽和转折为圆角，不使用字体字符 `✓`。深色圆形底上的勾固定使用 `--color-on-dark`，不能跟随卡片正文变成深色。
- 游戏类型卡应保留足够的视觉预览面积，不使用空心圆点作为唯一识别。
- 标签高度 24–28px，12px 圆角；按语义使用薄荷、天蓝、珊瑚或中性浅底。
- 标签只表达分类或状态，不能替代主要按钮。

### 内容卡片

- 默认白色或象牙纸色背景、20px 圆角，优先无边框并通过画布色差与留白分层；确需边界时才使用 1px 暖米色细线或 `--shadow-card`，不默认同时叠加两者。
- 重点贺卡、手帐和 Gift Reveal 容器使用 28px 圆角与 `--shadow-raised`。
- 折角属于大型主叙事卡的可选状态，不属于所有 `paper-surface` 的默认状态。只有完整场景、完整内容模板或页面主摘要可以增加折角；高度较低的横向提示条与附属信息卡不得增加折角。
- 卡片内边距：小卡 12–16px，普通卡 20px，主场景卡 24px。
- 卡片只用于真实层级：贺卡、游戏场景、礼物和分享链接，不把每个表单区块都装进卡片。

### 底部导航与图标

- 底部导航为白色 20px 圆角容器，顶部或整体使用 1px 暖米色边框。
- 默认图标和文字为暖棕；当前项使用珊瑚红。
- 图标统一使用细到中等线宽的同一图标家族，默认 20–24px。
- 不用 Emoji、文字字符或自制 CSS 图形代替正式 UI 图标。

Scrapbook 始终保持 3:4 固定模板，不变成自由编辑器。P0 为 2 个 Birthday Card 模板和 3 个 Scrapbook 模板；三种手帐分别容纳 1 张、2 张、3 张照片，不设置独立标题，只共享一个 0–20 字、最多两行的共同说明区。照片区域不叠加文字，Tada 与装饰不得进入说明或照片安全区。手帐可以参考实体剪贴簿的相纸、胶带、彩纸、星星和蜡笔涂鸦，但必须控制装饰密度、保留清楚的照片层级，并继续使用 Ta-da! 色板。Sender 中的模板选择统一使用左侧真实缩略图、右侧名称与简短说明的列表卡，不使用只有文字和单选圆点的选择方式。P0 照片自动填入固定槽位，并允许在槽内拖动主体、缩放与重置；槽位位置、大小、顺序和角度固定。纸底、用户照片、相纸边框 / 胶带、外侧贴纸 / 涂鸦、共同说明必须保持独立图层，装饰只压白边或外侧角落，不遮中心主体。贴纸移动、删除和自由画布属于 P1。

Scrapbook 三种模板的唯一视觉基准已经由 D-042 锁定，Sender 缩略图、Sender 实时预览、Receiver 海报和保存 PNG 必须一一对应，不得根据后续概念稿重构：

- 1 张照片：`/Users/sunny/.codex/generated_images/01a0499a-939c-7552-97c4-95ae387971b5/exec-61ffd9d0-f3ad-46e9-9d56-5ce7d4b06c19.png`
- 2 张照片：`/Users/sunny/.codex/generated_images/01a0499a-939c-7552-97c4-95ae387971b5/exec-bc47e6c9-8606-4643-9cdc-5265cd8d9b27.png`
- 3 张照片：`/Users/sunny/.codex/generated_images/01a0499a-939c-7552-97c4-95ae387971b5/exec-e6a8cfcb-2377-4ab1-9b6e-888b4b9e4943.png`

严格保留参考图中的暖奶油纸页、外缘装饰密度、相纸相对位置与比例、只压相纸白边的胶带 / 夹子和底部宽大撕纸共同说明区；无顶部标题、无逐照说明。Receiver 的保存与继续操作位于 3:4 海报外。后续生成的 `exec-13520a7c-39f3-4b2a-9a1e-aef6ba78c178.png` 已作废，不得作为实现参考。

未上传照片槽不得沿用 D-042 概念稿中的山、天空、草地、花篮等示意照片，统一显示米白纸张纹理；只有用户真正上传成功的槽位才显示照片。空槽纸纹、用户照片、白色相纸框和前景胶带 / 回形针 / 星星必须保持独立图层。空模板、部分上传、照片读取失败与草稿恢复缺图均遵守同一规则，同时保留相纸位置、角度、贴纸和说明区几何。

390×844 手机图只用于页面与排版审核，1200×1600 拆层图只用于当前构图验证和实现前资产分层，不属于最终高清交付。正式模板母版至少为 2400×3200（严格 3:4）；用户照片必须保留原始分辨率参与裁切，不得先压缩为审核截图尺寸；共同说明在 2400×3200 导出画布上实时排版绘制。Receiver 保存 PNG 固定输出 2400×3200，并在验收中提供照片边缘、纸纹、胶带和文字的 100% 局部清晰度检查。

## 动效顺序

动效在阶段 E 才加入，并必须表达以下至少一种含义：

- 层级：引导用户关注下一步
- 反馈：确认点击、找到或解锁成功
- 状态变化：展示礼盒打开或内容出现
- 故事节奏：让 Opening 和 Reveal 有情绪递进

只动画 `transform` 和 `opacity`。所有非必要动效尊重 `prefers-reduced-motion`，并保留静态完成态。

正式动效采用“先样片、后批量”的顺序：先完成 Gift Reveal 样片，用户审核角色一致性、情绪和节奏后，再制作 Opening、Unlock 和 Gift 三组核心动效。长距离走路、复杂肢体抓取和手绘逐帧形变属于 P1。

三段核心动效节奏已经冻结：Opening 约 1.45 秒，Unlock 成功约 1.1 秒，Gift Reveal 样片约 2.6 秒。每段只播放一次并停在可操作的静态完成态，不循环抢夺注意力。

Gift Reveal 样片顺序为：礼盒出现 → 用户点击 → 盒盖弹起 → Tada 升起 → 出现 `Ta-da!` → 6–10 片彩纸 → 礼物信息出现。样片不包含音乐、3D、复杂逐帧形变或大量粒子；Reduced Motion 直接显示打开后的静态完成态。

## 可访问性

- 正文与背景满足 WCAG AA 对比度。
- 所有主要按钮具有可见焦点、按下反馈和不换行标签。
- 点击热区至少 44×44px。
- 不能只用颜色表达游戏结果或完成状态。
- 插画提供描述性替代文本；纯装饰元素从辅助技术中隐藏。

## 明确禁止

- 紫蓝科技渐变
- 玻璃拟态、毛玻璃浮层
- 霓虹、外发光和粒子背景
- 大量渐变文字
- 默认 SaaS 仪表盘风格
- UI 普通组件使用粗棕描边与厚实下沉阴影
- 所有插画使用同样粗细的深棕轮廓
- 每个表单区块都套一张重卡片
- 每张卡片都折角、旋转、贴胶带或增加做旧纹理
- 重度泛黄、强噪点、褐色滤镜和影响阅读的纸面污渍
- 用空心单选圆点作为选择卡的主要视觉内容
- 六个场景分别发展不同视觉系统
- 未经审核直接制作复杂粒子、3D 或滚动劫持动画
- 用通用占位图长期冒充最终模板或游戏场景

## 视觉验收

- 五秒内能感受到“温暖、私人、生日”。
- 珊瑚红在全部页面保持相同语义。
- 主按钮、卡片和输入遵守轻盈 UI 体系；普通 UI 不使用粗棕描边，插画线条具有轻重层级。
- 深色表面统一反白，浅色表面统一使用深色文字，并通过对比度检查。
- 纸张质感可感知但不喧宾夺主；折纸和做旧只出现在有叙事意义的表面。
- 全局只出现 12 / 20 / 28 / 圆形四档圆角。
- 页面直接颜色必须来自本文件定义的令牌。
- 关闭动画后仍能完成全部流程。
- 任意单个模块替换后，不破坏整体视觉语言。
