# 2026-09-14 国内真实分享上线检查

目标：大陆普通网络、手机访问、可创建并分享自己的生日心意。00 串行修复，两个临时只读检查并行；未授权新云资源、真实用户数据、推送或部署。

## 已检查与本轮修复

- 浏览器五步创建普通贺卡 → 真实发布 → 新 Receiver 链接 → 贺卡 → 无礼物直接完成已走通；使用合成称呼，无真实个人数据。测试来源为 3004 运行副本，不能作为公网验收。
- 收回确认：此前已检查取消、Escape、焦点恢复、成功后旧链接刷新缺省、失败后可重试；360/390/430px 无横向溢出。
- 修复草稿 getItem 异常逃出捕获导致加载中断；清除草稿失败不会阻断重新创建。
- 照片异步读取增加等待保护：读取期间禁用换版式与底部继续，拒绝重入，避免按旧槽位写入新版式。
- 2026-09-15 使用仓库内合成图片完成照片链路回归：上传首图、打开裁切并调至 120%、完成裁切、从单图切为三图、补齐另外两图、发布、走完找礼物 / 回忆手帐 / 礼物 / 完成页。Receiver 的 3 个签名媒体请求全部 complete，天然尺寸分别为 720×538、720×538、1254×1254；未使用真实个人照片。
- 发布接口在读取大请求体之前验证配置中的可信 Origin 与 JSON 类型；拒绝缺失和跨站 Origin。未改分享契约。
- 发布错误区分服务故障、请求过大、频率限制、验证与冲突，不再全部描述为内容安全检查失败。
- 类型检查通过；定向 ESLint 0 error / 10 条既有 img warning；持久化与新增草稿测试 11/11 通过。完整运行副本生产构建另记 STATUS，不以旧构建替代新验证。

## 正式开放前的阻塞项（按执行顺序）

1. 完成微信、iPhone Safari、Android Chrome 手机真机验证；本地桌面浏览器的多宽度与照片回归不等于手机真机。
2. 提交明确文件范围并复核，确认最新代码快照，保留已上线固定 Demo 的回退版本。不得将临时 qa 路由或测试数据集成。
3. 服务器使用固定私有数据目录（如 /var/lib/tada），独立运行账户、目录 0700 / 文件 0600；生产两份独立密钥长期保留，不使用临时运行密钥。核对现有 systemd 与 Nginx，不凭本地测试推断其已配置。
4. 完整备份 records/public/media/blobs，密钥单独加密保管；在隔离目录恢复合成记录，证明公开链接与管理凭证可继续使用。回退代码不得删除数据。
5. Nginx 仅发布路径允许所需请求体大小，添加发布速率与并发限制、磁盘占用监测；核对敏感路径日志不保存 token、签名查询串、Cookie 或完整外部礼物 URL。
6. 核对域名实名 / ICP 备案实际状态，再配置 DNS 与可信 HTTPS origin，检查 Secure Cookie、证书和 HTTP 跳转。域名、备案、推送、服务器写入部署需在对应边界取得确认。
7. 域名上线后普通大陆网络走创建、发布、跨设备读取、撤回、旧链接失效、服务重启持久化与回退冒烟；通过后才记录“已发布”。

## 2026-09-15 待发布文件边界

- 运行代码：本轮实际变更的 `src/**`，包括无生日 Sender、发布校验、草稿恢复、作品列表、管理与收回界面；相关测试随提交保留，但不会被 Next 生产产物打包。
- 运行资产：更新后的三档本地字体、`tada-collection-cutout-v1.png`、`tada-holding-keepsake-v1.png` 及其来源说明。
- 明确排除：生成文件 `next-env.d.ts`、未被源码引用的 `tada-collection-quiet-v1.png`、`/tmp` 运行副本、合成记录 / 媒体 / 私钥、diagrams、设计稿和 QA 审核资料。
- 当前 main 工作树同时包含治理文档和历史资料，不能直接全量暂存或提交。正式提交需按上面边界逐项暂存、复核 staged diff，并保留现有公网固定 Demo 的回退版本。

## 本轮只读联网证据

- http://111.229.76.252/create 返回 200；这只能证明现有 HTTP + IP 入口在线，不证明含最新真实分享功能。
- 外部 DNS 查询 hellomiso.cn 未返回 A 记录；tada.hellomiso.cn 返回 NXDOMAIN。尚不能使用这些域名作为正式入口。
- 本机 DNS 返回 198.18.* 的代理合成地址，不能作为域名公网解析证据；ICP备案进度未知，不从 DNS 推断。

## 已批准限制，不扩大为本轮开发

- 撤回 / 到期停止内容和图片访问，不等于硬删除服务器文件；P0 无后台硬删除。
- 管理权依赖原浏览器 Cookie，换设备 / 清除 Cookie 后不提供找回；私有图片重新检查有效性。
- 自由输入子集外汉字可回退系统字体，不阻塞功能上线。
- 无礼物贺卡的按钮仍写“继续拆礼物”，本次看到但不影响直接完成；留给 05 确认低优先级文案调整。

本报告不是已发布证明，不能将自动测试、用户验收、GitHub 推送与公网部署合并描述为“完成”。
