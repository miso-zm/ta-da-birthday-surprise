# Ta-da! Receiver 与 Sender 流程

**状态：** Working 0.4（Gate A、Gate B 已通过，详细交互已冻结）

## Receiver 黄金路径

```text
打开 /s/[slug]
  ↓
加载惊喜数据
  ↓
Opening
  ↓ onOpen
Unlock Game（配置决定三种游戏之一）
  ↓ onComplete
Memory：Birthday Card
  ↓ onContinue
Memory：Scrapbook
  ↓ onContinue
Gift Reveal
  ↓ onReveal / onContinue
Share
```

Birthday Card 与 Scrapbook 属于同一个 Memory 阶段，但在黄金路径中依次展示，确保收礼人能看到两种内容；不得通过一个默认隐藏的标签页跳过其中之一。

## 进入与加载

- 路径：`/s/[slug]`。
- 读取类型化 `Surprise` 数据；当前使用本地样例，本地闭环验收后再替换为 Supabase 数据源。
- 加载时显示与页面结构一致的占位骨架，不使用全屏旋转图标。
- slug 不存在或数据无效时显示温和的错误页，并提供“重新加载”。
- 错误页不得展示未校验的外部链接或部分隐私内容。

## Opening

目标：让用户在 5 秒内明白这是专属生日惊喜，并主动开始。

- 显示收件人、发件人、短引导和一个主要按钮。
- 主要操作：`拆开看看`。
- 首次用户操作前不自动播放声音。
- `onOpen` 只负责通知流程进入 Unlock，不读取小游戏状态。

## Unlock Game

Sender 配置决定 Receiver 看到哪一种游戏；Receiver 正常流程中不显示游戏选择器。

### 找礼物（默认演示）

- 使用一个固定手绘房间，Sender 从柜子、沙发靠垫、花盆 3 个藏匿点中选择礼物位置。
- Receiver 场景提供约 5 个可点击热点，不包含人物走路、碰撞或地图。
- 点错物体时给出短提示，不扣次数。
- 连续点错 3 次后由 Tada 给出文字与方向提示，但不高亮或直接暴露答案。
- 点中礼物后显示明确完成反馈，并触发一次 `onComplete`。

### 剪刀石头布

- Sender 不需要额外配置。
- Receiver 选择石头、剪刀或布，系统产生对手结果。
- 获胜后完成；前两轮平局或失败可以重试，若仍未获胜则第三轮保证成功。
- 每轮必须明确读出双方选择和结果。

### 生日密码

- 密码自动读取 Sender 第一步填写的生日月日，不允许另设一套密码。
- Receiver 输入四位月日数字，演示值为 `0828`。
- 忽略空格和非数字格式字符后比较。
- 输入错误可以重试；连续两次错误后显示可读的生日月日，并明确提示输入四位 `MMDD`。
- 正确后触发一次 `onComplete`。

### 通用规则

- 三个游戏使用轻量 React 组件自行实现，不接入完整开源游戏项目。
- 单个游戏目标完成时间为 20–40 秒。
- 游戏完成回调具有幂等性，重复点击不能多次推进流程。
- 游戏内部不得直接导航到 Memory。
- 仅当游戏资源加载失败或运行时故障时提供“跳过并继续”的受控降级；正常答错、平局或失败不显示跳过。该事件需要可记录。
- 所有房间热点使用可聚焦按钮，并用可读文字同步结果；不得只用颜色、表情或插画变化表达状态。

## Memory：Birthday Card

- 展示模板、祝福正文、收发双方称呼。
- 祝福正文支持合理长度换行；过长数据在 Sender 阶段限制，不在 Receiver 静默截断。
- 阅读完成后通过唯一主按钮进入 Scrapbook。

## Memory：Scrapbook

- 固定 3:4 Template Composer，不提供自由画布。
- 模板定义背景、照片槽、装饰层和文字槽。
- 照片按槽位裁切并保持可读主体；无照片时显示设计完成的缺省槽，不显示破图。
- 阅读完成后进入 Gift Reveal。

## Gift Reveal

- 初始只显示未打开礼物和操作提示。
- 用户操作后显示礼物标题、描述和外部链接按钮。
- 外部地址必须为可解析的 `https://` URL，否则按钮禁用并显示解释。
- 不内嵌第三方购物页，不自动跳转。
- `onReveal` 记录已拆开状态；用户主动点击主按钮后进入 Share。

## Share

- 优先调用 Web Share API。
- 不支持或调用失败时提供复制当前分享链接。
- 复制成功或失败必须有可感知反馈。
- 分享取消视为正常结果，不显示错误警报。
- 页面保留重新查看惊喜的入口，但不重置已完成数据。

## 返回与恢复

- 浏览器刷新后，当前可以重新从 Opening 开始；Receiver 进度持久化不属于本轮静态方案。
- 浏览器返回键不得进入空白页或不可恢复状态。
- P0 不提供任意步骤导航；用户通过流程按钮向前推进。
- QA 可以使用开发态跳转工具测试模块，但工具不得出现在生产 Demo。

## Sender 六步（已审核）

```text
1. 基础信息与 Opening
  ↓
2. 选择并配置 Unlock Game
  ↓
3. Birthday Card
  ↓
4. Scrapbook
  ↓
5. Gift
  ↓
6. Preview / Publish
```

Sender 与 Receiver 共用 `Surprise` 数据契约，不复用 Receiver 的页面组件或流程状态。

- Preview 使用当前六步的本地草稿渲染完整 Receiver 主链路。
- 本地阶段的最终操作使用“保存并预览”，不伪造可分享 slug；Supabase 接入后再提供 Publish 和真实 `/s/[slug]`。
- 草稿自动保存在当前浏览器；返回上一步不得丢失已填写内容。
- 六步共用 390×844px 单列壳层：顶部固定返回、`N/6` 与保存状态，底部固定一个主操作；内容区独立滚动。
- 发现已有草稿时先显示恢复页，再进入第一个未完成步骤。
- Step 1：双方称呼、生日月日、Opening 短标题、引导文案和 2 个封面模板。
- Step 2：选择一个游戏；只有找礼物需要选择 3 个藏匿点之一。
- Step 3：选择 2 个 Card 模板之一，填写 10–240 字祝福和署名。
- Step 4：选择 2 个 3:4 模板之一，填写标题并上传 1–3 张照片；单张说明可选且不超过 30 字。
- Step 5：填写 2–40 字礼物名称、不超过 120 字描述和有效 HTTPS 链接。
- Step 6：不增加新内容；检查 Preview，分享标题与文案由双方称呼自动生成。
- 默认值用于快速形成完整演示：温暖 Opening、找礼物并藏在柜子、珊瑚 Card、三段回忆 Scrapbook。
- 照片按上传顺序自动填入模板槽位，仅支持替换与删除，不提供拖拽排序。
- Preview 打开完整 Receiver 主链路，但 Sender 无需在 Preview 中完成全部 Receiver 步骤即可结束创建。
- 六步顺序属于已批准决定，模块任务不得自行增加、删除或调换步骤。
