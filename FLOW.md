# Ta-da! Receiver 与 Sender 流程

**状态：** Main Flow 1.0 已保留；D-052 真实分享与跨设备阶段已批准

## Receiver 黄金路径

```text
打开 /s/[slug]
  ↓
加载惊喜数据
  ↓
Opening
  ↓
是否设置游戏？
  ├─ 是 → Unlock Game ─┐
  └─ 否 ───────────────┤
                       ↓
                 Memory（二选一）
                 ├─ Birthday Card
                 └─ Scrapbook
                       ↓
Gift Reveal
  ↓ onReveal / onContinue
Share
```

Receiver 不显示游戏选择器，也不显示未被 Sender 选择的另一种心意内容。`unlock.kind = none` 时从 Opening 直接进入 Memory；Memory 完成后统一进入 Gift Reveal。

## 进入与加载

- 路径：`/s/[slug]`。
- `/s/mia-birthday` 继续读取类型化本地样例；其他随机 Receiver 标识由服务端持久化加载层读取，不得回退为 Demo。
- 加载时显示与页面结构一致的占位骨架，不使用全屏旋转图标。
- slug 不存在或数据无效时显示温和的错误页，并提供“重新加载”；到期、撤回或已删除统一显示“这份惊喜已经收起来了”。
- 错误页不得展示未校验的外部链接或部分隐私内容。

## Opening

目标：让用户在 5 秒内明白这是专属生日惊喜，并主动开始。

- 显示收件人、发件人、短引导和一个主要按钮。
- 主要操作：`拆开看看`。
- 首次用户操作前不自动播放声音。
- `onOpen` 只负责通知流程进入 Unlock，不读取小游戏状态。

## Unlock Game

Sender 配置决定 Receiver 看到哪一种游戏；Receiver 正常流程中不显示游戏选择器。

### 找礼物（默认演示）

- 使用一张固定 4:3 手绘房间插画，房间内有三只可见礼物盒：沙发上的绿礼盒、花盆旁的黄礼盒和地毯上的粉礼盒。
- Sender 在插画上直接选择真正装有惊喜的一只礼物盒；Receiver 看到同一房间，只能点击三只礼物盒猜答案。
- 点错礼物盒时给出短提示，不扣次数；正确礼物盒打开后显示明确完成反馈。
- 连续点错 3 次后由 Tada 给出文字与方向提示，但不高亮或直接暴露答案。
- 点中礼物后显示明确完成反馈，并触发一次 `onComplete`。

### 剪刀石头布

- Sender 不需要额外配置。
- Receiver 选择石头、剪刀或布，系统产生对手结果。
- 获胜后完成；前两轮平局或失败可以重试，若仍未获胜则第三轮保证成功。
- 每轮必须明确读出双方选择和结果。

### 吹蜡烛

- Receiver 主动点击开始后才请求麦克风权限，不在页面加载时自动申请。
- 麦克风可用时，根据连续吹气的声音强度推进熄灭蜡烛反馈；瞬时噪声不能直接完成。
- 页面同时提供“按住吹气”作为可靠备用；按住达到完成条件后触发同一个 `onComplete`。
- 麦克风被拒绝、不可用或检测异常时，清楚说明原因并保留按住吹气，不形成死路。
- Sender 不需要配置麦克风参数或另外填写答案。

### 通用规则

- 三个游戏使用轻量 React 组件自行实现，不接入完整开源游戏项目。
- 单个游戏目标完成时间为 20–40 秒。
- 游戏完成回调具有幂等性，重复点击不能多次推进流程。
- 游戏内部不得直接导航到 Memory。
- 仅当游戏资源加载失败或运行时故障时提供“跳过并继续”的受控降级；正常答错、平局或失败不显示跳过。该事件需要可记录。
- 所有房间热点使用可聚焦按钮，并用可读文字同步结果；不得只用颜色、表情或插画变化表达状态。

## Memory：Birthday Card

- 展示模板、祝福正文、收发双方称呼。
- 祝福正文支持合理长度换行；过长数据在 Sender 阶段限制，不在 Receiver 静默截断。
- 阅读完成后通过唯一主按钮进入 Gift Reveal。

## Memory：Scrapbook

- 固定 3:4 Template Composer，不提供自由画布。
- 提供 1 张、2 张、3 张照片三种固定模板；模板定义背景、对应数量的照片槽、装饰层和一个共同说明槽，不设置独立标题槽。
- 照片按槽位裁切；Sender 可在每个固定槽内拖动画面、缩放和重置，Receiver 复用保存后的裁切结果。无照片时显示设计完成的缺省槽，不显示破图。
- 共同说明可选 0–20 字、最多两行。无论模板包含几张照片，都不显示标题或逐张照片说明。
- 照片区域不叠加文字；Tada、贴纸和其他装饰不得进入共同说明或照片安全区。
- 海报下方提供次要操作“保存这张海报”，导出严格 3:4 的手帐纸页 PNG；不导出整张手机页面、主按钮或纸页外的 Tada。
- 支持文件分享的设备优先分享 PNG；否则使用下载或可读的长按保存提示。生成中、成功和失败均有轻量文字反馈，失败可重试且不阻断主流程。
- 阅读完成后进入 Gift Reveal。

## Gift Reveal

- 初始只显示未打开礼物和操作提示。
- 用户操作后显示礼物标题、描述和外部链接按钮。
- 外部地址必须为可解析的 `https://` URL，且不得带 URL 用户名 / 密码或指向本地 / 内网目标，否则按钮禁用并显示解释。
- 不内嵌第三方购物页，不自动跳转。
- Ta-da! 只保存并展示外部礼物 URL，不保存电商账号、Cookie、地址、订单、付款、验证码或物流信息，也不声称第三方页面已经完成领取、付款或送达。
- `onReveal` 记录已拆开状态；用户主动点击主按钮后进入 Share。

## Share

D-061 更新：本阶段改为温暖结束页，动态显示收件人生日祝福、Tada 与静态彩纸；提供“再看一次”，有有效额外礼物时提供“查看礼物”。不显示 H5 链接、分享或复制操作。下列历史分享规则仅适用于 Sender 发布成功页，不再适用于 Receiver 终屏。

D-062 条件分支：Sender 若在心意内容步骤确认了生日主角海报，Share / 5/5 用最终合成海报替代 Tada 插画；未配置、旧数据或海报不可用时保持 D-061 的 Tada 回退。页面按钮和文字层级不因模板改变。

- 优先调用 Web Share API。
- 不支持或调用失败时提供复制当前分享链接。
- 复制成功或失败必须有可感知反馈。
- 分享取消视为正常结果，不显示错误警报。
- 页面保留重新查看惊喜的入口，但不重置已完成数据；分享使用当前 Receiver 链接，绝不携带 Sender 管理凭证。

## 返回与恢复

- 浏览器刷新后，当前可以重新从 Opening 开始；Receiver 进度持久化不属于本轮静态方案。
- 浏览器返回键不得进入空白页或不可恢复状态。
- P0 不提供任意步骤导航；用户通过流程按钮向前推进。
- QA 可以使用开发态跳转工具测试模块，但工具不得出现在生产 Demo。

## Sender 五步（已审核）

```text
1. 基础信息
  ↓
2. 惊喜玩法
  ↓
3. 心意内容（二选一）
  ↓
4. 礼物
  ↓
5. Preview / Publish
```

Sender 与 Receiver 共用 `Surprise` 数据契约，不复用 Receiver 的页面组件或流程状态。

- Preview 使用当前五步的本地草稿渲染 Receiver 条件链路。
- 本地 Preview 继续可用；真实分享阶段的最终操作发布不可变快照，并返回新的 `/s/[slug]`。重复点击通过幂等发布操作避免生成多份相同作品。
- 草稿自动保存在当前浏览器；返回上一步不得丢失已填写内容。
- 五步共用 390×844px 单列壳层：顶部固定返回、`N/5` 与保存状态，底部固定一个主操作；内容区独立滚动。
- 发现已有草稿时先显示恢复页，再进入第一个未完成步骤。
- Step 1：用户只填写双方称呼，两个输入项纵向排列；Opening 使用系统默认封面，标题和引导文案根据双方称呼自动生成，不提供额外设置。
- Step 2：在找礼物、剪刀石头布、吹蜡烛和直接送达中四选一；只有找礼物需要在房间插画中选择 3 只礼物盒之一。
- Step 3：先在普通贺卡与回忆手帐中二选一，再只填写所选内容。选择回忆手帐后进入单页实时 3:4 编辑，不增加独立模板页、逐图编辑页或检查页；默认一张照片模板、共同说明留空，模板与单图裁切仅在需要时通过底部面板打开。普通贺卡使用 2 个模板、10–200 字祝福和默认署名；回忆手帐使用 1 张、2 张、3 张照片三种固定 3:4 模板，只可选填写一句 0–20 字共同说明，并上传与模板对应数量的照片。模板面板使用左图右文的缩略图列表选择。
- Step 4：填写 2–40 字礼物名称、不超过 120 字描述和有效 HTTPS 链接。
- Step 5：不增加新内容；检查 Preview，分享标题与文案由双方称呼自动生成。
- 默认值用于快速形成完整演示：系统自动生成温暖 Opening、找礼物并选择沙发上的绿礼盒、默认选择珊瑚 Card；只有用户改选 Scrapbook 时才要求照片内容。
- 切换心意类型时可以保留另一类型的本地草稿，但 Preview 与发布只输出当前选择的一种。
- 照片按上传顺序自动填入所选模板的 1、2 或 3 个固定槽位；每张照片可在槽内拖动主体、缩放和重置，但不能拖拽排序、旋转、改变槽位或移动装饰；不为单张照片增加独立说明字段。
- Preview 打开条件式 Receiver 主链路，但 Sender 无需在 Preview 中完成全部 Receiver 步骤即可结束创建。
- 五步顺序属于已批准决定，模块任务不得自行增加、删除或调换步骤。

## D-052 发布、管理与失效流程

```text
Sender 本地草稿
  ↓ 校验文字、游戏、照片、裁切与外部 URL
上传并验证私有照片资源
  ↓
原子发布不可变快照
  ├─ 返回新的 Receiver 查看链接 /s/[随机标识]
  └─ 在独立 Sender 管理入口记录作品；管理权依赖服务端 HttpOnly 会话
        ↓
修改草稿后再次发布 → 生成另一条新链接
        ↓
Sender 确认新链接可用 → 可主动撤回旧链接
```

- Receiver 免登录、免密码。随机标识不可猜测，页面不在产品内公开展示并设置 `noindex`；任何持链接者仍可能查看和转发。
- Sender 管理入口与 Receiver 查看链接分开；管理凭证不得进入 Receiver URL、数据、分享内容或日志。当前 P0 不提供跨设备找回管理权。
- 每个快照默认有效一年。到期或撤回后立即停止新的内容读取与照片短时签名；Receiver 统一显示“这份惊喜已经收起来了”。后台删除时限按 00 推荐实施包另行确认，不计入 D-052 已批准规则。
- 发布失败保留本地草稿并允许重试；未知、部分上传、越权和服务错误不得伪装成发布成功。

## 主流程 1.0 逐屏契约

### Sender

| 屏幕 | 用户目标 | 必填内容 | 唯一主操作 | 输出 |
|---|---|---|---|---|
| 1 基础信息 | 说明惊喜送给谁 | 双方称呼 | `继续` | 自动生成 Opening 文案 |
| 2 惊喜玩法 | 决定是否需要解锁 | 四选一；找礼物时在插画中选择礼物盒 | `继续` | `unlock` 配置 |
| 3 心意内容 | 选择并完成一种心意；可选制作生日主角海报 | Card 祝福，或 Scrapbook 标题与照片；可选主角照片、模板及人物位置 | `继续` | `memory` 二选一内容；可选 `portrait` 最终海报 |
| 4 礼物 | 填写礼物去向 | 名称、描述、HTTPS 链接 | `继续` | `gift` 内容 |
| 5 预览与发布 | 检查收礼人实际体验 | 无新增字段 | 本地 `保存并预览`；真实分享阶段 `发布惊喜` | Receiver Preview 或新的不可变分享链接 |

### Receiver

| 屏幕 | 用户目标 | 唯一主操作 | 下一步 |
|---|---|---|---|
| Opening | 确认这是自己的生日惊喜 | `拆开看看` | 有游戏进入 Unlock；直接送达进入 Memory |
| Unlock | 在 20–40 秒内完成轻游戏 | 游戏内完成操作 | 当前选择的 Memory |
| Birthday Card | 阅读一句祝福 | `继续拆礼物` | Gift Reveal |
| Scrapbook | 浏览照片和回忆 | `继续拆礼物` | Gift Reveal |
| Gift Reveal | 主动打开并查看礼物 | `打开礼物`，打开后 `继续` | Share |
| Share | 分享或保存这份体验 | `分享这份惊喜` | 完成；复制链接作为降级 |

## 返回、校验与恢复

- Sender 返回上一步保留全部草稿；切换心意类型不立即删除另一类型内容。
- 每一步只校验当前可见且会进入发布结果的字段；未选择的 Card 或 Scrapbook 草稿不阻塞 Preview。
- Receiver 不提供任意步骤导航；浏览器返回不能进入空白或失效状态。
- 游戏和心意内容的分支由 Sender 数据决定，Receiver 不重新选择。
- 所有模块异常都必须保留可恢复路径；技术降级不得伪装成正常游戏结果。
