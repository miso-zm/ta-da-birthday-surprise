# Ta-da! 互动生日惊喜 H5：项目契约

**状态：** 固定 Demo 已具备；D-052 真实分享规则已批准；D-056 国内临时公网预览已发布，正式域名与真实分享仍待完成

**当前里程碑：** 保留现有 Sender 五步与 Receiver 条件链路；先完成 `hellomiso.cn` 的 ICP 备案、DNS 与 HTTPS，再实现不可变发布、跨设备读取与独立撤回管理

**项目类型：** Hackathon 可演示 Demo

**国内发布主体：** 个人（D-054）；已购买 `hellomiso.cn` 与腾讯云上海轻量应用服务器（D-055）。固定 Demo 已通过 HTTP + IP 部署到上海服务器（D-056）；ICP备案、域名解析与 HTTPS 尚未完成

## 产品目标

D-061：Receiver 最后一屏为生日祝福结束页，保留重看及有礼物时的查看礼物入口，不再显示 H5 链接、分享或复制按钮；分享职责保留在 Sender 发布完成页。

D-062：心意内容步骤可选上传收件人照片，在浏览器本地抠图并合成“气球派对”或“蓝调生日”主角海报；发布只保存最终 PNG。Receiver 最后一屏优先显示该海报，未配置和历史作品继续显示 Tada。原照片和中间抠图不上传。

让收礼人在手机上用不超过 2 分钟完成一次有情绪递进的生日惊喜：收到邀请、亲手解锁、阅读祝福与回忆、拆开外部礼物、分享体验。

Demo 首要服务于 Hackathon 现场评审，同时必须让普通收礼人无需讲解也能完成主链路。

## 品牌决定

- 产品名称固定为 `Ta-da!`。
- 产品 IP 名称为 `Tada`，定位为收藏心意、制造惊喜的小怪兽。
- Tada 是产品体验的一部分，需要覆盖 Opening、小游戏反馈、回忆、Gift Reveal 和 Share，但不能遮挡主要内容或取代清晰的操作提示。

## 成功标准

- Receiver 从 Opening 到 Share 可以连续完成，无死路或依赖口头解释的步骤。
- 首屏 5 秒内说明“这是给我的生日惊喜”和“下一步做什么”。
- 黄金路径在正常网络下 2 分钟内完成。
- 每个模块通过公共输入与回调连接，可以独立替换。
- 主要体验在 390×844px 完整可用，并覆盖 360-430px 手机宽度。
- 正式发布版本在中国大陆普通移动与家庭网络中无需 VPN 可访问；当前 `.vercel.app` Demo 不计入该验收。
- 主链路稳定之前，不让高级视觉、后台能力或复杂编辑拖慢进度。

## 用户与演示样例

- 主要用户：通过分享链接进入的生日收礼人。
- 次要用户：通过五步 Sender 创建惊喜并分享链接的送礼人。
- 演示内容使用本地样例 `Mia / Sunny`；真实创建能力接入后由数据替换，不在组件中写死姓名。
- 默认演示小游戏为“找礼物”，另外两种 P0 游戏通过相同配置接口切换。

## P0

### Receiver

- Opening 情绪开场
- Unlock Game：剪刀石头布、找礼物、吹蜡烛；也可选择直接送达
- Birthday Card
- 3:4 Template Composer Scrapbook
- Gift Reveal
- 仅支持外部 HTTPS 礼物链接
- Web Share API 分享，剪贴板作为降级
- 基本加载、错误、键盘和 Reduced Motion 支持

### Sender

- 创建一份惊喜
- 填写双方称呼；Opening 使用系统默认样式与自动文案
- 从三种解锁游戏和“直接送达”中四选一
- 从普通贺卡与回忆手帐中二选一，只填写所选内容
- 填写并校验 HTTPS 礼物链接
- 保留本地 Receiver Preview；真实分享阶段发布不可变快照并生成新的可分享链接

P0 模板范围：Opening 使用 1 个系统默认封面、Birthday Card 2 个模板、Scrapbook 3 个固定 3:4 模板；Scrapbook 分别容纳 1 张、2 张和 3 张照片，每份手帐不设置独立标题，只保留一句 0–20 字共同说明。

Receiver 可通过次要操作把严格 3:4 的 Scrapbook 手帐纸页保存为 PNG。导出内容只包含纸页内的照片、相纸边框、胶带 / 装饰与共同说明，不包含页面导航、操作按钮或纸页外的 Tada；保存失败不影响继续进入 Gift Reveal。

找礼物使用一张固定 4:3 手绘房间插画。房间内始终可见三只礼物盒：沙发上的绿礼盒、花盆旁的黄礼盒和地毯上的粉礼盒。Sender 选择真正装有惊喜的礼物盒；Receiver 在同一画面中猜中它才能解锁。礼物盒是游戏目标，不再让用户寻找柜子、靠垫或花盆本身。

Sender 的五步顺序已经冻结：

1. 基础信息
2. 惊喜玩法
3. 心意内容（二选一）
4. 礼物
5. Preview / Publish

五步内部字段与 `Surprise` 映射以 D-014 和 `PRD.md` 为准。Sender 草稿可以同时保留 Card 与 Scrapbook 的本地编辑内容，但 Preview 和发布结果只输出 `memoryKind` 当前选择的一种。草稿继续先保存在当前浏览器；发布成功后保留草稿，后续修改再次发布会生成新链接，不原地改写旧作品。

## D-052 真实分享规则

- Receiver 无需登录或密码，持不可猜测链接即可查看；链接不在产品内公开展示并设置 `noindex`，但持链接者仍可继续转发。
- Receiver 查看链接与 Sender 管理入口分离；管理凭证不得出现在 Receiver URL、数据、分享文案或日志。
- 每个发布结果是不可变快照，默认有效期一年。Sender 可提前撤回；到期或撤回后显示“这份惊喜已经收起来了”。
- 修改草稿后生成新链接；Sender 确认新链接可用后再撤回旧链接，旧链接不自动跳转。
- 电商只保存外部礼物 URL；不保存淘宝、京东、微信小店的账号、Cookie、地址、订单、付款、验证码或物流信息。
- P0 不新增账号登录或运营后台。Sender 管理依靠与 Receiver 链接分离的服务端管理会话；跨设备恢复管理权不属于当前 P0。

Gate B 已冻结移动端创建规则：390×844px 单列步骤页、固定顶部进度与保存状态、固定底部单一主按钮；默认内容可直接形成可预览结果。第一步仅收集双方称呼，不收集无产品用途的生日日期；照片按上传顺序自动填槽，并允许在固定槽内拖动主体、缩放、重置与替换。中断后进入恢复页并返回第一个未完成步骤。本地阶段提供真实 Receiver Preview，但不伪造分享 slug，也不要求 Sender 必须走完 Receiver 全流程才能结束创建。历史数据中的 `birthday` 字段仅作可选兼容保留。

## P1

- 拼照片小游戏
- 更多卡片和 Scrapbook 模板
- 模板内贴纸移动、删除等轻编辑
- 照片槽、照片顺序与装饰自由移动；P0 只允许在固定槽内调整照片主体位置与缩放，贴纸仍不可移动或删除
- 更丰富的 Opening 与 Gift Reveal 动效
- 音乐、声音和触觉反馈
- 数据统计、多人协作或模板市场

## 明确不做

- Canva 式自由画布编辑器
- 人物行走、碰撞、地图或复杂游戏引擎
- 站内支付、礼物购买或物流
- 非 HTTPS 外部链接
- 账号登录、运营管理台和跨设备找回管理权（当前 P0）
- 在主链路稳定前制作最终插画资产或复杂粒子动画

## 技术与发布默认值

- Next.js App Router、React、TypeScript
- Tailwind CSS；需要可访问基础组件时使用定制后的 shadcn/ui
- Motion 仅在静态页面通过后使用，并先完成 Gift Reveal 样片
- 第一阶段使用类型化本地样例数据
- 第二阶段推荐使用 Supabase Postgres + 私有 Storage 持久化并沿用 Vercel；具体账户、地区与费用边界确认后才创建外部资源
- Receiver 公共入口规划为 `/s/[slug]`
- Sender 入口规划为 `/create`
- 目标浏览器：近两代 iOS Safari、Android Chrome、桌面 Chrome

## 阶段门禁

1. Gate A 已通过：Sender 字段映射、小游戏边界和 P0 动效范围已冻结。
2. Gate B 已通过：小游戏状态和 Tada 场景系统已冻结；原 Sender 六步方案已由 D-014 的五步方案取代。
3. Gate C：先验收 Sender 五步 → Preview → Receiver 条件链路，再分别细化游戏、心意内容和 Gift / Share 静态体验。
4. Gate D 静态页面通过后，只制作一个 Gift Reveal 动效样片。
5. Gate E 样片通过后，才批量制作 Opening、Unlock 和 Gift 核心动效。
6. D-052 已批准真实分享阶段；先确认基础设施账户、地区与费用边界，再实现、独立 QA、用户验收，最后单独批准推送与 Vercel 部署。
