# Ta-da! 互动生日惊喜 H5：Agent 协作规则

## 当前执行补充（2026-09-09，D-050 / D-052 / D-053）

本节优先于下文旧阶段描述、长期模块任务名单及旧状态展示方式；不取消公共接口、单一写入者、验收与安全边界。

- 长期仅保留 `00 总控与版本状态` 和 `05 产品与设计决策`。00 独占共享文件、技术计划、集成、版本状态与发布安排；05 负责产品、设计和用户决策。准确任务 ID 见 STATUS.md。
- 下文的 10/15/20/30/40/50/60/90 是历史职责及目录边界索引，不是必须重建的长期任务。任务归档不等于其成果已集成，也不授权删除分支、工作树或未提交文件。
- 采用 `05 定义 → 00 计划 → 临时实现及 AI 自测、00 串行集成 → 用户验收`。默认串行；新任务 / Worktree 仍需用户明确授权，确需并行最多 3 个编码 Worktree，公共文件保持唯一写入者。
- 用户状态只展示：待定义、可开发、开发中、待验收、已验收、已发布。模块提交、复核、集成、预览来源和公网版本仍须作为证据分别记录，不能以“完成”合并这些事实。
- QA 不省略。一般由 00 做集成检查；重大或高风险变更按需申请临时独立 QA，不保留固定 90 任务。
- 当前已具备固定 Demo；D-052 已批准真实分享产品规则并授权临时任务 `C-真实分享与跨设备访问`。D-053 又将“中国大陆普通网络无需 VPN”设为正式发布目标，因此原 Vercel + Supabase 新加坡推荐包不再是默认输入；该任务继续只读等待自有域名、ICP备案与国内云路径确认。未经另行授权不得创建外部云项目、开通付费资源、写入真实用户数据、备案、迁移、推送或部署。
- 详细 SOP 见 `docs/AI_PRODUCT_DEVELOPMENT_WORKFLOW.md`。旧 Skill / 文档若与本节的协作安排冲突，以本节、最新已批准 DECISIONS 和 STATUS 为准；尚未批准的产品及技术建议不得因此生效。

## 当前阶段

Receiver 空壳主链路已作为结构基线保留，当前不处理不影响演示结构的小问题。**Gate A 与 Gate B 均已通过**；D-014 已将 Sender 更新为五步，并将 Birthday Card 与 Scrapbook 改为心意内容二选一。项目目前进入 **Gate C：主流程 1.0 与本地闭环验收**。

Gate C 先由总控冻结代码层公共契约和任务卡，再由用户明确授权创建编码任务与 Worktree。此阶段只实现本地 Sender → Preview → Receiver 闭环、小游戏和 Memory 结构，不制作最终插画或正式动效，不接入 Supabase。

用户不承担代码、类型或构建审核。冻结契约内的技术实现由总控直接推进并自检；只有可见 UI、真实操作流程、文案含义、Tada 形象与动效感受交给用户审核。

## 开始任何任务前

1. 首先阅读 `STATUS.md` 和 `DECISIONS.md`，再完整阅读本文件以及 `PROJECT.md`、`FLOW.md`、`MODULES.md`、`DESIGN.md`。
2. 在回复中说明本次任务名称、目标、允许修改范围和完成标准。
3. 检查当前 Git 状态和活动任务，确认没有其他任务正在修改相同文件。
4. 如果需求会改变黄金路径、公共模块接口、设计令牌或 P0/P1 范围，停止实现并交回 `00 总控与集成` 决策。

## 任务与权限

- `00 总控与集成` 独占：根目录共享文档、`STATUS.md`、`DECISIONS.md`、项目专属 Skill、应用入口、路由、流程状态机、公共类型、设计令牌、依赖和构建配置。
- `05 Ta-da! 品牌与动效规划`：Gate B 方案已通过；正式资产与动效仍等待后续对应关卡。
- `10 Receiver 空壳主链路`：只负责初始化工程、公共壳层和占位模块，不做最终视觉或业务细节。
- `15 Sender 五步与预览`：只负责 `src/features/sender/**` 内的五步创建体验和 Preview 数据准备，不接 Supabase、不修改路由。
- `20 Unlock Games`：只修改 `src/features/unlock/**` 及其模块测试。
- `30 心意内容体验`：只修改 `src/features/memory/**` 及其模块测试；Card 与 Scrapbook 是二选一分支，不得串联展示。
- `40 Gift Reveal 与 Share`：只修改 `src/features/gift/**`、`src/features/share/**` 及其模块测试。
- `50 视觉资产与动效`：只在已批准模块中替换资产或补充动效，不得改变模块接口。
- `60 Supabase 与真实链接`：本地 Sender → Preview → Receiver 闭环验收前不得启动。
- `90 移动端 QA 与部署`：默认只读；修复任务必须另行授权范围。

除 `00 总控与集成` 外，任何任务都不得自行修改：

- `AGENTS.md`、`PROJECT.md`、`FLOW.md`、`MODULES.md`、`DESIGN.md`
- `STATUS.md`、`DECISIONS.md`、`.agents/skills/tada-control/**`
- `src/app/**`
- `src/lib/surprise-contract.ts`
- `src/styles/tokens.css`
- `package.json`、锁文件和框架配置

## 并行规则

项目采用“并行模块线 + 串行集成队列”。Codex 中的任务彼此是平级工作空间；任务标题中的 `00`、`20`、`30` 表示项目职责，不代表系统会自动同步代码或自动形成父子控制关系。

- 互不重叠的模块可以在不同任务中同时进行设计、实现和模块预览，不必等待其他模块完成。
- 每个模块在 `STATUS.md` 中独立记录：`讨论中 → 方案已通过 → 实现中 → 模块已完成 → 总控已复核 → 已集成 → 统一预览已刷新 → 用户已验收`。这些状态不得合并书写；“方案已通过”不等于“代码已完成”，“模块已完成”也不等于“主链接已更新”。
- 同一模块目录同一时间只能有一个写入任务。一个设计方向如同时影响 Sender 和 Receiver，先由总控冻结跨模块信息结构，再分别交给对应目录所有者实现。
- 并行写代码必须使用独立 Git Worktree；一个 Worktree 对应一个结果明确的任务，同时进行的编码 Worktree 最多 3 个。纯讨论、静态概念稿和只读审核可以并行，但仍需标明所属模块和结果目标。
- 多个 Agent 不得同时修改共享入口、公共类型、全局样式或同一模块目录；子 Agent 优先承担只读调研、接口检查、测试、QA 和代码审查。
- 设计任务先交静态方案并通过审核，之后才允许实现；不同模块的静态设计审核可以同时进行。
- 所有完成的模块进入由 `00 总控与集成` 管理的单一集成队列。总控一次只集成一个结果，完成冲突检查和验证后才刷新统一验收预览。
- 用户拥有的 Codex 新任务不得由模块任务擅自创建；需要新任务或 Worktree 时，由总控提出明确任务卡并等待用户授权。

## 任务、代码与预览的交接

- `00 总控与集成` 是唯一权威状态入口；若同时存在多个名为“总控”的历史任务，以 `STATUS.md` 标注的当前总控任务为准，其他任务的结论必须交回当前总控后才生效。
- 模块任务结束时必须返回：任务名称、所属模块、工作目录 / Worktree、分支与提交号（没有提交必须明确写“未提交”）、修改文件、验证结果、模块预览地址、是否建议集成。
- 每个预览必须标注为以下两类之一：`模块预览` 只证明该任务自身的结果；`统一验收预览` 只由总控发布，并明确包含哪些已集成模块及对应代码快照。
- 模块任务中的保存、提交或预览不会自动进入主项目。只有总控完成复核、集成并刷新统一验收预览后，用户在主链接中才能看到该结果。
- 预览停止、来源目录变化或代码快照变化时，必须立即更新 `STATUS.md`，不得继续把旧地址描述为“当前最新版”。

## 审核关卡

- Gate A（已通过）：Sender 字段与 Receiver 映射、小游戏 P0 边界、Ta-da! P0 动效清单。
- Gate B（已通过）：小游戏完整状态、Ta-da! 场景分镜和资产清单；Sender 旧六步方案已由 D-014 取代。
- Gate C：本地 Sender 填写 → Preview → Receiver 完整主链路。
- Gate D：静态页面与 Ta-da! 出现场景。
- Gate E：Gift Reveal 动效样片。
- Gate F：Receiver 移动端情绪节奏和核心动效。
- Gate G：Supabase、真实分享链接、移动端 QA 与部署。

只有用户明确表示“通过”“进入下一阶段”或“开始实现”，才视为通过审核关卡。讨论、提问和要求查看不等于批准。

## 实现顺序

1. 先静态完成态。
2. 再补完整交互状态。
3. 再补错误、空状态和降级。
4. 最后补有明确沟通目的的动效。

不得为了动效改变流程、公共接口或内容结构。

## 每个模块必须覆盖的状态

- 默认状态
- 用户操作中的反馈状态
- 完成状态
- 可恢复的错误状态
- `prefers-reduced-motion` 降级状态（存在动效时）

## 完成汇报格式

每个任务结束时必须提供：

1. 实际完成内容
2. 修改文件范围
3. 可复现的演示步骤
4. 类型检查、测试、构建或截图证据
5. 未完成项与风险
6. 是否建议进入总控集成

## 完成定义

- 只修改授权范围内的文件。
- 模块遵守 `MODULES.md` 的输入与输出，不读取其他模块内部状态。
- 360-430px 宽度可用，390×844px 为主要验收尺寸。
- 键盘焦点、可读文本、按钮对比度和基本屏幕阅读器标签完整。
- 没有科技感渐变、玻璃拟态、霓虹发光或未经批准的视觉语言。
- 相关检查通过，已知限制明确记录。
